// create function make sure it returns jsx
//hardcode everything first
// 2 state valuers for current size and currenmt language
// size buttons, and corresponding 

const Greeting = ({greeting, setGreeting}) => {

    return (
        <header>
            <h1>{greeting}</h1>
        </header>
    );
};

export default Greeting